# v0.90 Small-screen panel fit

Built from v0.89.

Changes:
- Keeps v0.89 restored design.
- Keeps v0.89 stabilization hook.
- Adds tighter small-screen breakpoints to prevent clipped panels when switching to smaller screens.
- Uses existing measured `--wf-vw` / `--wf-vh`.
- Hides the right rail only if the iframe becomes too narrow to fit all 3 columns.

Server syntax check: `True`

```text

```
