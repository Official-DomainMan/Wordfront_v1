# Wordfront v1.0 Layout Foundation

Built from: `wordfront_v089_design_restored_stabilized.zip`

Based on the Discord Activity iframe model and responsive layout/ResizeObserver guidance:
- app responds to the iframe/container dimensions
- ResizeObserver only sets CSS variables/classes
- CSS Grid/Flex performs layout
- no transform scaling
- no fixed canvas
- no forced 16:9 wrapper

Handlers:
- deploy handler: `deployWord`
- clear handler: `clearPending`

Server syntax check: `True`

```text

```
